# =============================================================
# fb-comment-scraper — Template env vars (OPSIONAL, non-default)
# Salin file ini menjadi .env lalu isi bila ingin auto-login.
# JANGAN pernah commit .env (sudah di-gitignore).
# =============================================================

# Opsional: auto-login sekali pakai dengan konfirmasi user (FR-8).
# Jika dikosongkan, aplikasi selalu memakai login manual headful.
FB_EMAIL=
FB_PASSWORD=
